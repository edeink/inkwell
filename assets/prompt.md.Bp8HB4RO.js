import{_ as o,c,o as d,ai as i}from"./chunks/framework.Ckc8SVM3.js";const x=JSON.parse('{"title":"项目协作提示（供 AI 助手）","description":"","frontmatter":{},"headers":[],"relativePath":"prompt.md","filePath":"prompt.md"}'),l={name:"prompt.md"};function r(t,e,a,s,n,u){return d(),c("div",null,[...e[0]||(e[0]=[i('<h1 id="项目协作提示-供-ai-助手" tabindex="-1">项目协作提示（供 AI 助手） <a class="header-anchor" href="#项目协作提示-供-ai-助手" aria-label="Permalink to &quot;项目协作提示（供 AI 助手）&quot;">​</a></h1><h2 id="项目目的" tabindex="-1">项目目的 <a class="header-anchor" href="#项目目的" aria-label="Permalink to &quot;项目目的&quot;">​</a></h2><ul><li>实现类似 Flutter 的 UI 构建 → 布局 → 绘制流水线，用于图形编辑与可视化渲染。</li><li>以声明式 JSON/JSX 模板定义 Widget 树，驱动跨渲染引擎的绘制（Canvas2D、PixiJS、Konva）。</li><li>编辑器将组件树解析、布局为确定尺寸后，再调用渲染器进行像素级绘制。</li></ul><h2 id="技术栈" tabindex="-1">技术栈 <a class="header-anchor" href="#技术栈" aria-label="Permalink to &quot;技术栈&quot;">​</a></h2><ul><li>核心：<code>TypeScript</code> 严格模式、模块化（<code>ESNext</code>）、Vite 构建、React 19 用于演示页面。</li><li>渲染器：<code>Canvas2D</code>、<code>PixiJS</code>、<code>Konva</code>，统一遵循 <code>IRenderer</code> 接口。</li><li>模板转换：通过统一的 JSX 编译器将 JSX 转为组件 JSON 数据。</li></ul><h2 id="关键架构" tabindex="-1">关键架构 <a class="header-anchor" href="#关键架构" aria-label="Permalink to &quot;关键架构&quot;">​</a></h2><ul><li>编辑器入口：<code>Editor.create(containerId, options)</code>，从 JSON 渲染组件树并完成布局与绘制。 <ul><li>解析与渲染流程参见 <code>src/editors/graphics-editor.tsx:176</code>。·</li></ul></li><li>Widget 抽象基类：统一 <code>layout</code>（计算尺寸与子项定位）与 <code>paint</code>（绘制自身与子项）。 <ul><li>方法入口参见 <code>src/core/base.ts:206</code>（<code>layout</code>）与 <code>src/core/base.ts:285</code>（<code>paint</code>）。</li></ul></li><li>渲染器统一接口：定义跨引擎必须实现的方法，供 <code>paint</code> 调用。 <ul><li>接口定义参见 <code>src/renderer/IRenderer.ts:23</code>。</li></ul></li></ul><h2 id="渲染器接口约定-irenderer" tabindex="-1">渲染器接口约定（IRenderer） <a class="header-anchor" href="#渲染器接口约定-irenderer" aria-label="Permalink to &quot;渲染器接口约定（IRenderer）&quot;">​</a></h2><ul><li>必选方法： <ul><li><code>initialize(container, options)</code>：初始化并挂载画布。</li><li><code>resize(width, height)</code>：调整画布尺寸。</li><li><code>render()</code>：渲染一帧（立即或驱动 ticker）。</li><li><code>destroy()</code>：释放资源并清空容器。</li><li><code>getRawInstance()</code>：返回原始引擎实例（用于高级控制）。</li></ul></li><li>绘制相关： <ul><li><code>save()</code> / <code>restore()</code> / <code>translate(x, y)</code>：坐标系状态管理；<code>Widget.paint</code> 依赖此约定进行子项偏移。</li><li><code>drawText(...)</code> / <code>drawRect(...)</code> / <code>drawImage(...)</code>：基础绘制原语，所有 Widget 绘制通过此层调用。</li></ul></li><li>参考实现： <ul><li>Canvas2D 初始化与绘制实现：<code>src/renderer/canvas2d/Canvas2DRenderer.ts:19</code>、<code>src/renderer/canvas2d/Canvas2DRenderer.ts:185</code>、<code>src/renderer/canvas2d/Canvas2DRenderer.ts:238</code>、<code>src/renderer/canvas2d/Canvas2DRenderer.ts:271</code>。</li><li>PixiRenderer 初始化与绘制实现：<code>src/renderer/pixi/PixiRenderer.ts:29</code>、<code>src/renderer/pixi/PixiRenderer.ts:170</code>、<code>src/renderer/pixi/PixiRenderer.ts:247</code>、<code>src/renderer/pixi/PixiRenderer.ts:304</code>。</li><li>KonvaRenderer 初始化与绘制实现：<code>src/renderer/konva/KonvaRenderer.ts:17</code>、<code>src/renderer/konva/KonvaRenderer.ts:148</code>、<code>src/renderer/konva/KonvaRenderer.ts:186</code>、<code>src/renderer/konva/KonvaRenderer.ts:214</code>。</li></ul></li></ul><h2 id="widget-系统规范" tabindex="-1">Widget 系统规范 <a class="header-anchor" href="#widget-系统规范" aria-label="Permalink to &quot;Widget 系统规范&quot;">​</a></h2><ul><li>类型与注册： <ul><li>每个 Widget 子类需在静态初始化中注册类型，类型字符串大小写敏感，需与 JSON 中 <code>type</code> 匹配。 <ul><li>例：<code>Text</code> 注册参见 <code>src/core/text.ts:55</code>；<code>Container</code> 注册参见 <code>src/core/container.ts:55</code>。</li></ul></li><li>通过 <code>WidgetRegistry.createWidget(data)</code> 按 <code>data.type</code> 查找构造器并创建实例（<code>src/core/base.ts:115</code>）。</li></ul></li><li>生命周期方法： <ul><li><code>createChildWidget(childData)</code>：从子项数据递归创建 Widget（<code>src/core/base.ts:200</code>）。</li><li><code>performLayout(constraints, childrenSizes)</code>：返回自身 <code>Size</code>，并由基类完成子项定位（<code>src/core/base.ts:276</code>）。</li><li><code>paintSelf(context)</code>：仅绘制自身内容，坐标原点为当前组件左上角；子项绘制与坐标偏移由基类处理（<code>src/core/base.ts:311</code>）。</li></ul></li><li>坐标与绘制： <ul><li>基类在绘制子项前调用 <code>renderer.save()</code>、<code>renderer.translate(dx,dy)</code>，绘制完成后 <code>renderer.restore()</code>（<code>src/core/base.ts:291</code>～<code>src/core/base.ts:305</code>）。</li><li>Widget 的 <code>paintSelf</code> 内必须以局部坐标系（0,0）进行绘制。</li></ul></li><li>布局与约束： <ul><li>使用 <code>BoxConstraints</code> 统一约束（最小/最大宽高），并由每个 Widget 决定传递给子项的约束（<code>getConstraintsForChild</code>）。</li><li>Flex 系列： <ul><li><code>Column</code> 与 <code>Row</code> 支持 <code>MainAxisAlignment</code>、<code>CrossAxisAlignment</code>、<code>MainAxisSize</code> 与 <code>spacing</code> 等（<code>src/core/flex/column.ts:31</code>、<code>src/core/flex/row.ts:26</code>）。</li><li>通过 <code>Expanded</code> 与子项 <code>flex</code> 属性分配主轴空间，<code>Tight</code> 表示强制占满、<code>Loose</code> 可小于分配值（<code>src/core/flex/type.ts:36</code>、<code>src/core/flex/expanded.ts:21</code>）。</li></ul></li><li>典型容器：<code>Container</code>、<code>Padding</code>、<code>Center</code>、<code>Stack</code>、<code>SizedBox</code> 等，分别处理尺寸、边距、居中、层叠与固定尺寸。</li></ul></li></ul><h2 id="错误与约束-flutter-风格" tabindex="-1">错误与约束（Flutter 风格） <a class="header-anchor" href="#错误与约束-flutter-风格" aria-label="Permalink to &quot;错误与约束（Flutter 风格）&quot;">​</a></h2><ul><li>不允许在主轴约束为无限（<code>unbounded</code>）时使用具有非零 <code>flex</code> 的子项： <ul><li>典型报错由 <code>createRenderFlexUnboundedError(&#39;vertical&#39;|&#39;horizontal&#39;)</code> 生成（<code>src/core/flex/errors.ts:22</code>）。</li><li><code>Column</code> 在 <code>maxHeight</code> 无限且存在 <code>flex</code> 子项且 <code>mainAxisSize</code> 为 <code>max</code> 时抛错（<code>src/core/flex/column.ts:136</code>～<code>src/core/flex/column.ts:143</code>）。</li></ul></li><li>文本度量：优先使用 Canvas 精确测量，不可用时退化为估算方法（<code>src/core/text.ts:113</code> 与 <code>src/core/text.ts:205</code>）。</li></ul><h2 id="模板与数据" tabindex="-1">模板与数据 <a class="header-anchor" href="#模板与数据" aria-label="Permalink to &quot;模板与数据&quot;">​</a></h2><ul><li>JSX → JSON 转换：在演示与测试中以 JSX 定义结构，转换后交由编辑器渲染。 <ul><li>运行时：自定义 <code>jsx-runtime</code>/<code>jsx-dev-runtime</code>（<code>src/utils/jsx-runtime.ts</code>、<code>src/utils/jsx-dev-runtime.ts</code>）。</li><li>编译器：<code>src/utils/jsx-compiler.ts</code>（识别 <code>*Element</code> 命名并映射到真实组件类型）。</li><li>兼容入口：<code>src/utils/jsx-to-json.tsx</code> 复用新编译器提供 <code>jsxToJson</code>/<code>createTemplate</code>。</li><li>编辑器模板渲染：<code>Editor.renderTemplate</code> 直接使用新编译器（<code>src/editors/graphics-editor.tsx</code>）。</li><li><code>src/test/data.tsx</code> 顶部按文件使用 <code>/** @jsxImportSource ../utils */</code> 指向自定义运行时。</li><li>组件类型枚举来源：<code>src/editors/graphics-editor.tsx:28</code>。</li><li>示例数据：完整演示场景见 <code>src/test/data.tsx</code>，测试页面入口见 <code>src/test/test-page.tsx</code>。</li></ul></li></ul><h2 id="代码风格与工程约束" tabindex="-1">代码风格与工程约束 <a class="header-anchor" href="#代码风格与工程约束" aria-label="Permalink to &quot;代码风格与工程约束&quot;">​</a></h2><ul><li>TypeScript：<code>strict: true</code>，<code>noUnusedLocals/Parameters: true</code>，<code>moduleResolution: &#39;bundler&#39;</code>，<code>verbatimModuleSyntax: true</code>。 <ul><li>配置参见 <code>tsconfig.app.json:2</code> 与 <code>tsconfig.node.json:2</code>。</li></ul></li><li>ESLint：使用 <code>@eslint/js</code>、<code>typescript-eslint</code> 推荐规则，React Hooks 与 Vite 刷新插件；无自定义规则。 <ul><li>配置参见 <code>eslint.config.js:8</code>～<code>eslint.config.js:22</code>。</li></ul></li><li>命名与文件： <ul><li>类与组件类型使用 <code>PascalCase</code>，文件名常见 <code>camelCase.ts</code> 或明确模块名。</li><li>组件 <code>type</code> 字段大小写敏感，需与注册值一致（常为 <code>PascalCase</code>）。</li></ul></li><li>依赖管理：<code>pnpm</code> 均可；开发脚本：<code>dev</code>、<code>build</code>、<code>preview</code>、<code>lint</code>、<code>test</code>（<code>package.json</code>）。 <ul><li>测试：新增 <code>vitest</code> 与单元测试样例（<code>src/utils/__tests__/jsx-compiler.spec.tsx</code>）。</li></ul></li></ul><h2 id="为此项目编写代码的建议-ai-编码规范" tabindex="-1">为此项目编写代码的建议（AI 编码规范） <a class="header-anchor" href="#为此项目编写代码的建议-ai-编码规范" aria-label="Permalink to &quot;为此项目编写代码的建议（AI 编码规范）&quot;">​</a></h2><ul><li><p>严格遵守 <code>IRenderer</code> 与 <code>Widget</code> 约定：任何新渲染器或组件都必须实现接口并注册类型。</p></li><li><p>新增 Widget 的步骤：</p><ul><li>创建子类，实现 <code>createChildWidget</code>、<code>performLayout</code>、<code>paintSelf</code>、必要的约束与定位方法。</li><li>在静态初始化中调用 <code>Widget.registerType(&#39;&lt;TypeName&gt;&#39;, Class)</code>，确保与 JSON <code>type</code> 一致。</li><li>若需要在演示环境自动注册，确认在 <code>src/core/registry.ts</code> 有侧向 import 以触发静态注册。</li></ul></li><li><p>绘制坐标处理：在 <code>paintSelf</code> 中使用相对坐标（0,0）；子项偏移由基类统一管理。</p></li><li><p>布局策略：优先基于约束与子项尺寸推导自身尺寸，避免在未确定自身尺寸时计算子项绝对位置。</p></li><li><p>处理图片与文本：</p><ul><li>图片需考虑 <code>fit</code> 与 <code>alignment</code>（<code>src/core/image.ts:177</code>）。</li><li>文本需进行行分割与省略号处理，并保证与约束一致（<code>src/core/text.ts:134</code>、<code>src/core/text.ts:176</code>）。</li></ul></li><li><p>错误与日志：沿用当前日志与错误风格，尽量在问题发生处提供清晰的中文提示与栈信息。</p></li><li><p>开发与测试</p></li><li><p>演示页面：<code>src/test/test-page.tsx</code> 提供“完整流程测试”与“渲染器测试”两套路径。</p><ul><li>渲染器选择与初始化参见 <code>src/test/test-page.tsx:147</code>～<code>src/test/test-page.tsx:155</code>。</li><li>完整流程（Build → Layout → Paint）参见 <code>src/test/test-page.tsx:163</code>～<code>src/test/test-page.tsx:172</code>。</li></ul></li><li><p>本地分辨率设置：<code>LOCAL_RESOLUTION</code> 用于渲染分辨率（<code>src/utils/localStorage.ts:92</code>）。</p></li><li><p>单元测试：运行 <code>pnpm test</code>，验证 JSX 编译输出一致性与属性映射。</p></li></ul><h2 id="扩展点" tabindex="-1">扩展点 <a class="header-anchor" href="#扩展点" aria-label="Permalink to &quot;扩展点&quot;">​</a></h2><ul><li>可新增渲染器实现，遵循 <code>IRenderer</code> 并在编辑器中选择。</li><li>可扩充 Widget 类型（如形状、路径、富文本），遵循现有布局与绘制契约。</li><li>可为布局系统补充更多对齐、约束组合与错误诊断文本。</li></ul><hr><p>此提示旨在让 AI 助手快速理解项目结构与编码约束，从而安全、正确地进行协作开发与功能扩充。</p>',23)])])}const h=o(l,[["render",r]]);export{x as __pageData,h as default};
